spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    jdbc-url: jdbc:log4jdbc:mysql://localhost:3306/spring_batch?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: kim111
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy

  main:
    web-application-type: none